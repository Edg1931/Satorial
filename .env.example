ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# Local: ./data/satorial.db (default).
# Vercel/serverless: app auto-detects and uses /tmp/satorial.db (ephemeral).
# Railway/Render/Fly.io with persistent volume: e.g. /data/satorial.db
SATORIAL_DB_PATH=./data/satorial.db
